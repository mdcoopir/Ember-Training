
import { round } from 'menu-tracker/helpers/round';
import { module, test } from 'qunit';

module('Unit | Helper | round');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = round([42]);
  assert.ok(result);
});

