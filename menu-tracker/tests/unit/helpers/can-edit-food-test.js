
import { canEditFood } from 'menu-tracker/helpers/can-edit-food';
import { module, test } from 'qunit';

module('Unit | Helper | can edit food');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canEditFood([42]);
  assert.ok(result);
});

