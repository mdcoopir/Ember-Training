
import { ratingBar } from 'chef-tracker/helpers/rating-bar';
import { module, test } from 'qunit';

module('Unit | Helper | rating bar');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = ratingBar([42]);
  assert.ok(result);
});

