
import { canEditChef } from 'chef-tracker/helpers/can-edit-chef';
import { module, test } from 'qunit';

module('Unit | Helper | can edit chef');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = canEditChef([42]);
  assert.ok(result);
});

